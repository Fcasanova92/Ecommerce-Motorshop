import { MainLayout } from "@/layouts/MainLayout";

export const Product = () => {
  return (
    <MainLayout>
    <main>
        <section id="item-viewer">
            <div class="headline">
                <h2 class="title-b">Visor de Productos</h2>
            </div>
        </section>
    </main>
    </MainLayout>
  );
};
